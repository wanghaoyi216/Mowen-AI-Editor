# 2026-05-31 OpenRouter 主调用层接入实施记录

## 本轮目标

1. 将 OpenRouter 作为 AI 主调用方接入项目
2. 实时获取当前免费模型
3. 为后续模型轮询与章节生成打下基础

## 本轮完成内容

1. 新增 OpenRouter 配置：
   - `OPENROUTER_API_KEY`
   - `OPENROUTER_BASE_URL`
2. 新增 OpenRouter 客户端封装。
3. 新增免费模型发现与轮询选择逻辑。
4. 新增基础聊天调用封装。
5. 新增接口：
   - `GET /api/v1/openrouter/models/free`
6. 将 Trend ReAct 执行链接入 OpenRouter 文本生成步骤。
7. 完成一次真实模型列表调用验证。

## 实时验证结果

已成功实时获取到 OpenRouter 当前免费模型列表，并自动选中了一个当前可用免费模型。

本轮实时测试得到的示例选择结果：

- `nousresearch/hermes-3-llama-3.1-405b:free`

说明：

1. 这不是写死模型，而是运行时动态发现。
2. 后续每次调用都可以按当前免费池重新轮询选择。

## 当前意义

这一步让项目真正具备了：

1. 实时发现可用免费模型
2. 避免绑定单一模型
3. 使用 OpenRouter 作为统一模型网关

这对后续章节生成、角色扩写、剧情规划、总结抽取都非常重要。

## 当前限制

1. 当前轮询策略还是轻量级随机优先策略。
2. 还没有基于任务类型做模型路由。
3. 还没有对模型失败做自动重试和回退。

## 下一步建议

1. 为不同任务类型设计不同模型偏好
2. 增加免费模型失败回退链
3. 将章节设计与章节生成接入 OpenRouter 调用
4. 将 OpenRouter 输出写回任务步骤和资产生成链
